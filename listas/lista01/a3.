#include <stdio.h>
#include <string.h>

typedef struct {
   char produto[50];
   float preco;
   int quantidade;
} Produto;

int main(){
     Produto a1;

     scanf("%s %f %d", a1.produto, &a1.preco, &a1.quantidade);

     
     
}